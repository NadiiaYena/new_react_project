function Error() {
    return (
        <div className="component">
            <div className="error">
                <p>Error 404. Такої сторінки не існує. Перевірте адресу.</p>
            </div>
        </div>
    );
}

export default Error;